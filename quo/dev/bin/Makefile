# =====================================================================
# (c) Copyright 2001  BBNT Solutions, LLC
# =====================================================================

LINKS := Node
LINKS += QuoNode
LINKS += SSLNode
LINKS += AdaptDemoNode
LINKS += CorbaNode
LINKS += ComboNode
LINKS += FutileNode
LINKS += LoggingNode
LINKS += MulticastNode
LINKS += NoisyNode
LINKS += PreSerializeNode
LINKS += RemoteSSLNode
LINKS += SecureNode
LINKS += SeqNode
LINKS += SerializeNode
LINKS += SimpleNode
LINKS += StatNode
LINKS += StepNode
LINKS += TraceNode
LINKS += TrafficMaskNode



install: $(LINKS)

Node:
	ln -s wrapper Node

QuoNode:
	ln -s wrapper QuoNode

SSLNode:
	ln -s wrapper SSLNode

AdaptDemoNode:
	ln -s wrapper AdaptDemoNode

CorbaNode:
	ln -s wrapper CorbaNode

ComboNode:
	ln -s wrapper ComboNode

FutileNode:
	ln -s wrapper FutileNode

LoggingNode:
	ln -s wrapper LoggingNode

MulticastNode:
	ln -s wrapper MulticastNode

NoisyNode:
	ln -s wrapper NoisyNode

PreSerializeNode:
	ln -s wrapper PreSerializeNode


RemoteSSLNode:
	ln -s wrapper RemoteSSLNode

SecureNode:
	ln -s wrapper SecureNode

SeqNode:
	ln -s wrapper SeqNode

SerializeNode:
	ln -s wrapper SerializeNode

SimpleNode:
	ln -s wrapper SimpleNode

StatNode:
	ln -s wrapper StatNode

StepNode:
	ln -s wrapper StepNode


TraceNode:
	ln -s wrapper TraceNode

TrafficMaskNode:
	ln -s wrapper TrafficMaskNode

clean:
	rm -f $(LINKS)

